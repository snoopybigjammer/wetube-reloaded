extends base.pug
include mixins/video

block content
    div.profile__data
        if !loggedInUser.avatarUrl
            span 😀
        else if loggedInUser.avatarUrl.startsWith("http")
            img(src=loggedInUser.avatarUrl, width="150", height="150").profile__link
        else
            img(src="/" + loggedInUser.avatarUrl, width="150", height="150").profile__link
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found

