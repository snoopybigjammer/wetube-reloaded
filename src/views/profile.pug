extends base.pug
include mixins/video

block content
    div.profile__data
        if !loggedInUser.avatarUrl
            span 😀
        else
            img(src=loggedInUser.avatarUrl, width="150", height="150", crossorigin).profile__link
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message No videos found

